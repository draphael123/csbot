{
  "name": "escalation-service",
  "version": "1.0.0",
  "description": "Document query service for customer support",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "postinstall": "cd frontend && npm install"
  },
  "keywords": ["customer-support", "document-query", "escalation"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mammoth": "^1.6.0",
    "natural": "^6.10.3",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

